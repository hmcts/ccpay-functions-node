apiVersion: v1
kind: Service
metadata:
  name: {{ include "rig.fullname" . }}
  labels:
    app: {{ include "rig.fullname" . }}
spec:
  selector:
    app: {{ include "rig.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.port }}
      targetPort: http